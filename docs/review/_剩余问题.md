# Review 剩余问题（总览）

> **review 进度抬头**
> 上次 review 结尾 commit：`f95719f`（2026-07-06 第三次 review，覆盖 M3 全量 `0c65f11..f95719f`）
> 下次 review 默认从 `f95719f` 之后的新 commit 接起。
>
> **2026-07-07 开发会话已清 backlog**：M1 #1–6、M2 #7–13、M3 #14–19 共 19 条，除 #7 作
> 取舍外全部改掉，落在 `eb52589..16300d8`（六个 fix commit，下次 review 覆盖这段）。
> 每条的解决状态 + commit 见各单次记录顶部的「解决状态」表。
>
> 历次 review：[M1（`5e74c66..cff3707`）](./2026-07-06-M1-core-strategy.md) · [M2（`cff3707..0c65f11`）](./2026-07-06-M2-data-backtest-report-cli.md) · [M3（`0c65f11..f95719f`）](./2026-07-06-M3-scan.md)

## ① 还没解决的问题

（暂无——2026-07-07 已全部清理。）

## ② 取舍记录（决定不改的）

1. **#7 `_covers` 只按首末日期判断覆盖，检测不出区间内数据空洞** —— `loader.py`。
   彻底修需要一份交易日历（按「区间内应有多少交易日 vs 实际根数」校验），离线环境下
   没有可靠日历源，**暂不接入完整校验**。已把限制写进 `data/loader.py` 模块 docstring
   与 `_covers` 注释；`refresh=True` 可强制重下绕过缓存。落在 `d8a73d4`。
   详见 [M2 记录 #1](./2026-07-06-M2-data-backtest-report-cli.md#1)。
