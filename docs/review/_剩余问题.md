# Review 剩余问题（总览）

> **review 进度抬头**
> 上次 review 结尾 commit：`f95719f`（2026-07-06 第三次 review，覆盖 M3 全量 `0c65f11..f95719f`）
> 下次 review 默认从 `f95719f` 之后的新 commit 接起。
>
> 历次 review：[M1（`5e74c66..cff3707`）](./2026-07-06-M1-core-strategy.md) · [M2（`cff3707..0c65f11`）](./2026-07-06-M2-data-backtest-report-cli.md) · [M3（`0c65f11..f95719f`）](./2026-07-06-M3-scan.md)

## ① 还没解决的问题

### M1 遗留（strategy + core，经 M2、M3 review 确认仍未处理）

1. **跌买触及资金上限用 `continue` 而非 `break`** —— `engine.py:174`。注释和 `desired_orders`（`engine.py:149-150`）都是 break 语义，唯独 `step` 这处是 continue；在 `down_amount_factor<1` + 紧 cap 下会买出非连续持仓，且 `step` 与 `desired_orders` 决策漂移。详见 [M1 记录 #1](./2026-07-06-M1-core-strategy.md#1)。
2. **`desired_orders` 是只读查询却改 ladder 状态** —— `engine.py:137` 调 `ensure_covers_down_to` 永久延伸阶梯、推进 `_next_gap`/`_ext_depth`，污染后续 `step` 与未来持久化状态。详见 [M1 记录 #2](./2026-07-06-M1-core-strategy.md#2)。
3. **`extend_levels_down` 纯函数只被测试调用，生产走 `Ladder._extend_one`** —— 两套平行实现已分叉（纯函数不算每格金额放大）。`gridlines.py:68` vs `ladder.py:106`。详见 [M1 记录 #3](./2026-07-06-M1-core-strategy.md#3)。
4. **Python 版本文档/工具链对不齐** —— roadmap `roadmap.md:74` 写 3.12，实际锁 3.13；`requires-python=">=3.12"` 也比工具链目标宽松。详见 [M1 记录 #4](./2026-07-06-M1-core-strategy.md#4)。
5. **M1 测试覆盖缺口** —— 等比模式引擎层零用例、`upper_rebuild_ratio` 中间值未测、两个 start/step 守卫未测、`down_amount_factor<1` 未测（问题 1 的盲区）。详见 [M1 记录 #5](./2026-07-06-M1-core-strategy.md#5)。
6. **`pyproject.toml` 幽灵 modified（CRLF 噪音）** —— `core.autocrlf=true` 且无 `.gitattributes`，根因仍在。详见 [M1 记录 #6](./2026-07-06-M1-core-strategy.md#6)。

### M2 新发现（data + backtest + report + cli，经 M3 review 确认仍未处理）

7. **`_covers` 按首末日期判断覆盖，检测不出区间内数据空洞** —— `loader.py:62-66`。缓存里若留有缺失日，之后请求被误判命中、返回残缺数据，回测静默跑在残缺行情上。详见 [M2 记录 #1](./2026-07-06-M2-data-backtest-report-cli.md#1)。
8. **分钟线 akshare 路径零测试、也没真实跑通** —— `akshare_provider.py:52-59` 的 `_fetch_minute` 无测试，cli.md 注明真实下载被代理拦。核心周期路径未验证。详见 [M2 记录 #2](./2026-07-06-M2-data-backtest-report-cli.md#2)。
9. **CLI 完全没做错误处理** —— `cli/app.py`，fetch / backtest / **scan** 三条路径都裸奔，akshare/配置/扫描异常直接 traceback。详见 [M2 记录 #3](./2026-07-06-M2-data-backtest-report-cli.md#3)。
10. **买入持有分母(cost) ≠ 网格总收益分母(initial_cash)** —— `metrics.py:156`，对照列口径不一致，小本金下偏差明显。详见 [M2 记录 #4](./2026-07-06-M2-data-backtest-report-cli.md#4)。
11. **年化(365 日历日) vs 夏普(252 交易日) 日计数混用** —— `metrics.py`，两个"年化"口径不同。详见 [M2 记录 #5](./2026-07-06-M2-data-backtest-report-cli.md#5)。
12. **缓存写入非原子** —— `cache.py:57` 直写目标路径，中断留半残 parquet。详见 [M2 记录 #6](./2026-07-06-M2-data-backtest-report-cli.md#6)。
13. **单次 fetch 内重复 ts 会让 `BarSeries` 抛错** —— `provider.py` 不去重 + `BarSeries` 要求严格递增；分钟线跨段拼接偶发重复时间戳会崩。详见 [M2 记录 #7](./2026-07-06-M2-data-backtest-report-cli.md#7)。

### M3 新发现（scan 参数扫描）

14. **calmar 排序被"不交易"配置刷分** —— `runner.py:57-59` 在 `max_drawdown==0` 时无条件返回极大值，不看回报；ZERO 模式 + 行情不触网格的配置会 0 成交、0 回撤、0 回报，却拿 `10^18` 排第一，被当"最优"。直接削弱 M3 的价值，且会传导给 M4 模拟盘。详见 [M3 记录 #1](./2026-07-06-M3-scan.md#1)。
15. **未知 vary 字段被静默忽略 + 渲染时崩** —— `GridConfig.from_dict` 忽略未知键，`vary` 字段名拼错会让该维度无声失效（产出重复 config），`render_scan_report` 里 `getattr` 取不到字段还会 AttributeError 崩。详见 [M3 记录 #2](./2026-07-06-M3-scan.md#2)。
16. **fixed 与 vary 字段同名时 fixed 被静默覆盖** —— `space.py:53-57`，vary 后写直接盖掉 fixed，不告警。详见 [M3 记录 #3](./2026-07-06-M3-scan.md#3)。
17. **`--top` 传负数切出错误切片** —— `markdown.py:19` 的 `rows[:top_n]`，负数会丢末行，终端还打印"前 -1 组"。详见 [M3 记录 #4](./2026-07-06-M3-scan.md#4)。
18. **run_scan 主路径无测试 + 长扫描无进度反馈** —— `test_runner.py` 只测排序、没真跑 `run_scan`；扫描串行且组合多时终端长时间静默。详见 [M3 记录 #5](./2026-07-06-M3-scan.md#5)。
19. **vary 候选值重复不去重** —— `space.py:53`，重复值产出重复 config、`size` 虚高、报告里出重复行。详见 [M3 记录 #6](./2026-07-06-M3-scan.md#6)。

## ② 取舍记录（决定不改的）

（暂无）
