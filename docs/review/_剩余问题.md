# Review 剩余问题（总览）

> **review 进度抬头**
> 上次 review 结尾 commit：`cff3707`（2026-07-06 首次 review，覆盖 M1 全量 `5e74c66..cff3707`）
> 下次 review 默认从 `cff3707` 之后的新 commit 接起。

## ① 还没解决的问题

1. **跌买触及资金上限用 `continue` 而非 `break`** —— `engine.py:173-174`。注释和 `desired_orders`（`engine.py:149-150`）都是 break 语义，唯独 `step` 这处是 continue；在 `down_amount_factor<1` + 紧 cap 下会买出非连续持仓，且 `step` 与 `desired_orders` 决策漂移。详见 [2026-07-06-M1](./2026-07-06-M1-core-strategy.md#1)。
2. **`desired_orders` 是只读查询却改 ladder 状态** —— `engine.py:137` 调 `ensure_covers_down_to` 永久延伸阶梯、推进 `_next_gap`/`_ext_depth`，污染后续 `step` 与未来持久化状态。详见 [2026-07-06-M1](./2026-07-06-M1-core-strategy.md#2)。
3. **`extend_levels_down` 纯函数只被测试调用，生产走 `Ladder._extend_one`** —— 两套平行实现已分叉（纯函数不算每格金额放大）。`gridlines.py:68` vs `ladder.py:106`。详见 [2026-07-06-M1](./2026-07-06-M1-core-strategy.md#3)。
4. **Python 版本文档/工具链对不齐** —— roadmap 写 3.12，实际锁 3.13；`requires-python=">=3.12"` 也比工具链目标宽松。详见 [2026-07-06-M1](./2026-07-06-M1-core-strategy.md#4)。
5. **M1 测试覆盖缺口** —— 等比模式引擎层零用例、`upper_rebuild_ratio` 中间值未测、两个 start/step 守卫未测、`down_amount_factor<1` 未测（问题 1 的盲区）。详见 [2026-07-06-M1](./2026-07-06-M1-core-strategy.md#5)。
6. **`pyproject.toml` 永远幽灵 modified（CRLF 噪音）** —— `core.autocrlf=true` + 工作区 LF，建议加 `.gitattributes` 并 renormalize。详见 [2026-07-06-M1](./2026-07-06-M1-core-strategy.md#6)。

## ② 取舍记录（决定不改的）

（首次 review，暂无）
