# Review 剩余问题（总览）

> **review 进度抬头**
> 上次 review 结尾 commit：`43629d5`（2026-07-07 第四次 review，覆盖 `f95719f..43629d5` 全量 60 commit）
> 下次 review 默认从 `43629d5` 之后的新 commit 接起。
>
> **2026-07-07 开发会话已清掉前三次 review 的 #1–#19**（除 #7 取舍外全部改对，本次 review 逐条核实修复正确，详见各单次记录的「解决状态」表）。下面 ① 只列本次新发现的 #20–#37。
>
> 历次 review：[M1（`5e74c66..cff3707`）](./2026-07-06-M1-core-strategy.md) · [M2（`cff3707..0c65f11`）](./2026-07-06-M2-data-backtest-report-cli.md) · [M3（`0c65f11..f95719f`）](./2026-07-06-M3-scan.md) · [M4+ store/paper/web/data（`f95719f..43629d5`）](./2026-07-07-store-paper-web-data.md)

## ① 还没解决的问题

### 高（关系回测数字对不对）

20. **默认日线源 sina 不复权；缓存键不区分 source/adjust** —— `akshare_provider.py:48,65` 默认走 sina（`fund_etf_hist_sina` 无 adjust、返回原始价），ETF 分红日会有"凭空暴跌"K 线；缓存键 `{symbol}_{frame}.parquet` 不带复权维度，跨源/跨基准合并会让拼接处价格跳空、`_covers` 还判成命中。详见 [M4+ 记录 #20](./2026-07-07-store-paper-web-data.md#20)。

### 中（持久化账本可审计 / web 鲁棒性）

21. **`process_tick` 非原子事务** —— `paper/runner.py:86-94` + `repository.py:38,61`，save_tick / 每个 save_fill 各自 commit，崩一半 → fills 表缺行，`snapshot.n_fills` 与 `sum(fills.realized_pnl)` 和引擎内存状态发散，静默。详见 [#21](./2026-07-07-store-paper-web-data.md#21)。
22. **tick 用 ts 当 PK + ORDER BY ts** —— `store/db.py:18`，时钟回拨 / 同 ts 撞车让 replay 顺序错乱、孤儿 fills 残留。详见 [#22](./2026-07-07-store-paper-web-data.md#22)。
23. **akshare NaN 让报价接口崩或渗进 JSON** —— `web/quotes.py:54,65,72`，`Decimal(str(NaN))`=`Decimal('NaN')` 不被 try 拦，一个坏行使整个 `/api/quotes`/`watchlist/enriched` 空白。详见 [#23](./2026-07-07-store-paper-web-data.md#23)。
24. **GET `/api/state?db=...` 建文件 + 路径不校验** —— `web/routes/state.py:22-23`，GET 副作用建空 sqlite 库，`db=../../foo.sqlite` 可行。详见 [#24](./2026-07-07-store-paper-web-data.md#24)。
25. **改运行中策略配置静默脱节、无重部署** —— `web/strategy_deploy.py:87-125`，文件/内存引擎/DB 三者脱节，再 deploy 抛 FileExistsError。详见 [#25](./2026-07-07-store-paper-web-data.md#25)。
26. **strategies create/update 只 catch ValueError** —— `web/routes/strategies.py:67-81`，缺字段抛 KeyError → 500（应 400）。详见 [#26](./2026-07-07-store-paper-web-data.md#26)。
27. **portfolio/strategies 的 `mode` 参数未校验** —— `routes/portfolio.py:22,28` + `strategies.py:49`，非法 mode → 500。详见 [#27](./2026-07-07-store-paper-web-data.md#27)。
28. **etf_info akshare 失败未捕获 + NaN 名称进缓存** —— `web/etf_info.py:33-41`。详见 [#28](./2026-07-07-store-paper-web-data.md#28)。
29. **腾讯 qfq 静默退回 day（不复权）** —— `tencent_provider.py:72`，qfqday 缺/空时 fall back 到不复权、不告警。详见 [#29](./2026-07-07-store-paper-web-data.md#29)。

### 低（工程健壮性 / 防御）

30. **mypy 没全绿** —— `test_stress.py`/`test_scan_api.py` 测试侧类型标注偷懒（`_report()->object`），16 个 mypy 错；违反"门禁全绿才 commit"。详见 [#30](./2026-07-07-store-paper-web-data.md#30)。
31. **多 writer 无 busy_timeout，web 层还有裸 connect 不开 WAL** —— `store/db.py:39` + `web/portfolio.py:154`，并发写易 `database is locked`。详见 [#31](./2026-07-07-store-paper-web-data.md#31)。
32. **部署 TOCTOU 竞态** —— `strategy_deploy.py:100-113`，检查与写入两段、无锁。详见 [#32](./2026-07-07-store-paper-web-data.md#32)。
33. **etf_info 模块级缓存非线程安全** —— `etf_info.py:33-41`，FastAPI 线程池下冷缓存重复刷新 + 读到半填缓存。详见 [#33](./2026-07-07-store-paper-web-data.md#33)。
34. **`_resolve_price` 接受 inf/-inf** —— `routes/ladder.py:38-45`，过 400 校验进引擎、行为未定义。详见 [#34](./2026-07-07-store-paper-web-data.md#34)。
35. **fills 表无自然键 UNIQUE** —— `store/db.py:22`，防御纵深缺口（当前非活跃）。详见 [#35](./2026-07-07-store-paper-web-data.md#35)。
36. **`PaperRunner.__init__` 不自动 replay** —— `paper/runner.py:47-65`，API footgun（CLI/测试都调了 replay）。详见 [#36](./2026-07-07-store-paper-web-data.md#36)。
37. **mootdx 读 `full["volume"]` 依赖 ≥0.10，pyproject 却写 ≥0.9** —— `mootdx_provider.py:63`，版本约束与代码假设不自洽。详见 [#37](./2026-07-07-store-paper-web-data.md#37)。

## ② 取舍记录（决定不改的）

1. **#7 `_covers` 只按首末日期判断覆盖，检测不出区间内数据空洞** —— `loader.py`。
   彻底修需要一份交易日历（按「区间内应有多少交易日 vs 实际根数」校验），离线环境下
   没有可靠日历源，**暂不接入完整校验**。已把限制写进 `data/loader.py` 模块 docstring
   与 `_covers` 注释；`refresh=True` 可强制重下绕过缓存。落在 `d8a73d4`。
   详见 [M2 记录 #1](./2026-07-06-M2-data-backtest-report-cli.md#1)。
